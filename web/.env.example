# 实验室签到系统前端环境变量模板
#
# 用法：复制为 web/.env 后按需取消注释修改（web/.env 已被 git 忽略，不会入库）。
# 仅在 npm run dev 的开发服务器代理中生效；生产环境由同源反向代理转发 /api，
# 无需额外配置。

# 开发服务器 /api 代理目标（默认 http://localhost:3000，即本地后端）
# VITE_API_PROXY_TARGET=http://localhost:3000
