# 开发环境默认配置（本文件入库且无密钥，全员共用）
# 覆盖方式：系统环境变量优先于本文件，如 PORT=3001 npm run dev
NODE_ENV=development

# 后端端口（默认 3000，由环境代码提供；取消注释可覆盖）
# PORT=3000
# SQLite 数据库文件（默认 server/data/lab-checkin.db）
# DB_PATH=
# 允许的前端来源（默认放行所有来源，本地开发无需设置）
# CORS_ORIGIN=http://localhost:5173
