# 测试环境默认配置（本文件入库且无密钥，全员共用）
# 启动：cd server && npm run dev:test，配合前端 npm run dev:test（5174 端口）
# 默认端口 3100、独立测试库 server/data/lab-checkin-test.db，均由环境代码提供
NODE_ENV=test

# 覆盖端口 / 数据库时取消注释
# PORT=3100
# DB_PATH=server/data/lab-checkin-test.db
