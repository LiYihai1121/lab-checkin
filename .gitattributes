# 统一换行与差异处理：仓库内一律 LF，由 Prettier/.editorconfig 同规则保障
* text=auto eol=lf

# Windows 脚本必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.db binary
*.woff binary
*.woff2 binary
